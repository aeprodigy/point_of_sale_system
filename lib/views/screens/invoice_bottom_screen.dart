import 'package:flutter/material.dart';

class InvoiceBottomScreen extends StatelessWidget {
  const InvoiceBottomScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Invoice '),),
    );
  }
}